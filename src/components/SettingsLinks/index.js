import React,{Component} from 'react';
import {Container} from '@material-ui/core';

export default class SettingsLinks extends Component{
    render(){
        return(
            <Container maxWidth='sm'>
            
            </Container> 
        )
    }
}